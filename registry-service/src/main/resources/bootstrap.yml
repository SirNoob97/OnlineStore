spring:
  application:
    name: registry-service
  cloud:
    config:
      uri: http://localhost:8081
      username: root
      password: root

server:
  error:
    include-message: always
    include-binding-errors: always
