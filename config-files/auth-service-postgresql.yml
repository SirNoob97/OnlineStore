jwt:
  expiration:
    time: ${JWT_EXPIRATION:1200000} #20 min

envs:
  postgres:
    hostname: ${POSTGRES_HOSTNAME:localhost}
    port: ${POSTGRES_PORT:5432}
    database: ${POSTGRES_DB:store}
    user: ${POSTGRES_USER:postgres}
    password: ${POSTGRES_PASSWORD:postgres}
