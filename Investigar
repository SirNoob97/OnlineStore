{
  "productId": 3,
    "productNumber": 9632512,
    "productName": "Samsung",
    "productDescription": "Personal Camera",
    "productStock": 20,
    "productPrice": 300.45,
    "createAt": "2010-01-01",
    "category": {
      "categoryId": 3,
      "categoryName": "Camera"
    },
    "subCategories": {
      "scanAvailable": true,
      "prefetch": -1
    }
}

debido a que r2dbc no tiene un soporte para relaciones entre entidades toca hacer las consultas con sql en el codigo despues de pensarlo bien decidi que solo product_service usaria
programacion reactiva, al momento de obtener las subCategories, que es una relacion muchos a muchos con los productos, no consegui la respuesta me aparecen esos campos en el json
lo que no comprendo es que al momento de hacer un log al flujo antes de retornar al controlador si puedo observar las subCategories en la consola, intente obtenerlas  de manera bloqueante
usando una lista pero siempre necesiataba usar el flujo de productos para obtenerlas por el id correspondiente pero no consegui nada supongo que debi usar un handler en lugar de un
restcontroller pero al ser la respuesta un json supongo que de igual manera no puedo obtener un json de un flujo dentro de otro.

dejare la clase de las sub categorias y la tabla con las relaciones a como las cree talvez en el futuro puedo encontrar respuesta para este problema.

si veo que los problemas de este tipo persisten con el resto del micro servicio separare este servicio del proyecto para investigar despues que termine la app.

consulta para obtener las sub categorias por el id del producto
//		final String querySubCategories = "SELECT * FROM sub_categories JOIN sub_categories_products ON sub_categories_products.sub_category_id = sub_categories.sub_category_id "
//				+ "WHERE sub_categories_products.product_id = :productId;";
